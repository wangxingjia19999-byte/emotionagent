# 心语陪伴后续开发计划与提示词

> 项目名称：心语陪伴：基于阿里云百炼大模型的情绪陪伴与社交互助平台  
> 当前范围说明：本阶段不开发 AI agent，不接入阿里云百炼智能体能力。AI 情绪陪伴可以先保留为首页入口或占位页面，但不实现模型调用、情绪分析、智能回复等业务逻辑。

## 一、当前项目状态

根据现有代码，项目已经具备以下基础：

- 后端使用 FastAPI + SQLAlchemy + MySQL + JWT。
- 已有用户注册、登录、获取资料、修改资料、修改密码相关接口。
- 已有 `users` 用户表模型。
- 前端使用 Vue3 + Vite + Element Plus + Axios。
- 已有登录页、注册页、首页基础页面。
- Axios 已封装 Token 自动携带和错误提示。
- 首页 `Home.vue` 已改造成登录后的核心入口页，包含：
  - 顶部导航栏
  - 欢迎区域
  - AI 情绪陪伴入口
  - 好友聊天入口
  - 社区广场入口
  - 今日情绪小贴士

当前项目离完整系统还缺少：

- 前端路由补全。
- 个人中心页面。
- 社区广场、帖子详情、发布帖子页面。
- 好友列表、好友申请、私聊页面。
- 后端社区模块接口。
- 后端好友与私聊模块接口。
- 后台管理模块。
- README、运行说明和测试流程。
- `.env.example`、`.gitignore`、初始化 SQL 等工程整理。

## 二、开发原则

后续开发需要严格遵守以下边界：

- AI agent 不在当前开发任务内。
- 好友聊天只是用户和用户之间的普通聊天，不调用 AI。
- 社区广场只是普通论坛功能，不调用 AI。
- 首页只负责展示、路由跳转、登录状态判断和退出登录。
- 不要把 AI 聊天消息、私聊消息、社区评论混在同一张表里。
- 数据库继续使用 MySQL，不切换 SQLite。
- 前端页面保持温柔、现代、治愈、简洁，不做默认模板风格。

## 三、推荐后续开发顺序

### 第 1 步：工程整理与路由补全

目标：

- 补齐前端页面路由。
- 增加缺失页面的基础空壳。
- 增加 `.gitignore`。
- 避免继续提交 `node_modules`、`dist`、`__pycache__`、日志文件。

需要完成：

- 创建或补全以下页面：
  - `frontend/src/views/Profile.vue`
  - `frontend/src/views/Friends.vue`
  - `frontend/src/views/PrivateChat.vue`
  - `frontend/src/views/Community.vue`
  - `frontend/src/views/PostDetail.vue`
  - `frontend/src/views/PublishPost.vue`
  - `frontend/src/views/Admin.vue`
- 在 `frontend/src/router/index.js` 中增加路由。
- 对主要页面增加登录拦截。
- 创建 `.gitignore`。

提示词：

```text
请基于当前 Vue3 + Vite + Element Plus 项目补全前端路由和基础页面。

要求：
1. 修改 frontend/src/router/index.js。
2. 增加以下页面文件：
   - Profile.vue
   - Friends.vue
   - PrivateChat.vue
   - Community.vue
   - PostDetail.vue
   - PublishPost.vue
   - Admin.vue
3. 每个页面先做基础占位结构，风格要和 Home.vue 保持一致，温柔、现代、浅色渐变、圆角卡片。
4. 路由包括：
   /profile
   /friends
   /private-chat
   /community
   /community/:id
   /publish-post
   /admin
5. 未登录用户访问主要页面时跳转到 /login。
6. 不要开发 AI agent，不要接入阿里云百炼。
7. 新增 .gitignore，忽略 node_modules、dist、__pycache__、*.log、.env。
8. 保持代码结构清晰，可以直接运行。
```

验收标准：

- 前端路由可以正常跳转。
- 未登录访问主要页面会回到登录页。
- 页面不会报错。
- 生成文件不会污染 Git。

## 四、第 2 步：个人中心模块

目标：

- 完成用户资料展示和修改。
- 支持修改昵称、头像、密码。
- 展示“我的帖子、我的收藏、我的好友”入口。

前端页面：

- `frontend/src/views/Profile.vue`

可复用接口：

- `GET /api/user/profile`
- `PUT /api/user/profile`
- `PUT /api/user/password`

提示词：

```text
请开发个人中心页面 frontend/src/views/Profile.vue。

要求：
1. 使用 Vue3 script setup + Element Plus。
2. 页面风格与 Home.vue 保持一致：浅色渐变背景、毛玻璃卡片、圆角 18px 到 24px、轻微阴影。
3. 从后端 GET /api/user/profile 获取用户信息。
4. 显示头像、用户名、昵称、邮箱、角色。
5. 支持修改昵称和头像，调用 PUT /api/user/profile。
6. 支持修改密码，调用 PUT /api/user/password。
7. 增加功能入口卡片：
   - 我的帖子
   - 我的收藏
   - 我的好友
   - 返回首页
8. 点击退出登录时清除 localStorage 中的 token 和 user，并跳转 /login。
9. 表单需要校验，接口错误需要提示。
10. 不要加入 AI agent 逻辑。
```

验收标准：

- 用户能查看资料。
- 用户能修改昵称和头像。
- 用户能修改密码。
- 页面视觉统一。

## 五、第 3 步：社区后端模块

目标：

- 完成普通社区论坛功能后端。
- 不调用 AI。

新增文件建议：

- `backend/app/models/post.py`
- `backend/app/schemas/post.py`
- `backend/app/routers/posts.py`

新增表：

- `posts`
- `comments`
- `likes`
- `favorites`

提示词：

```text
请基于当前 FastAPI + SQLAlchemy + MySQL 项目开发社区模块后端。

要求：
1. 新增 backend/app/models/post.py。
2. 新增 backend/app/schemas/post.py。
3. 新增 backend/app/routers/posts.py。
4. 数据库表包括：
   - posts：帖子表
   - comments：评论表
   - likes：点赞表
   - favorites：收藏表
5. 所有主键使用 BigInteger，自增。
6. 内容字段使用 Text，支持中文和 emoji。
7. 所有接口需要 JWT 登录鉴权，帖子列表和帖子详情可以允许登录用户访问。
8. 实现接口：
   POST   /api/posts
   GET    /api/posts
   GET    /api/posts/{post_id}
   PUT    /api/posts/{post_id}
   DELETE /api/posts/{post_id}
   POST   /api/posts/{post_id}/comments
   GET    /api/posts/{post_id}/comments
   DELETE /api/comments/{comment_id}
   POST   /api/posts/{post_id}/like
   DELETE /api/posts/{post_id}/like
   POST   /api/posts/{post_id}/favorite
   DELETE /api/posts/{post_id}/favorite
   GET    /api/posts/my
   GET    /api/posts/favorites
9. 支持分类筛选、关键词搜索、最新排序、热度排序。
10. 用户只能编辑和删除自己的帖子、自己的评论。
11. 社区模块是普通论坛功能，不调用 AI，不做情绪分析，不生成 AI 评论。
```

验收标准：

- 用户能发帖、看帖、评论、点赞、收藏。
- 用户不能修改别人的帖子。
- 社区接口不包含 AI 调用。

## 六、第 4 步：社区前端页面

目标：

- 完成社区广场、发布帖子、帖子详情。

页面：

- `frontend/src/views/Community.vue`
- `frontend/src/views/PublishPost.vue`
- `frontend/src/views/PostDetail.vue`

建议组件：

- `frontend/src/components/PostCard.vue`
- `frontend/src/components/EmptyState.vue`

提示词：

```text
请开发社区前端页面，基于当前 Vue3 + Vite + Element Plus 项目。

要求：
1. 新增或完善：
   - frontend/src/views/Community.vue
   - frontend/src/views/PublishPost.vue
   - frontend/src/views/PostDetail.vue
   - frontend/src/components/PostCard.vue
   - frontend/src/components/EmptyState.vue
2. 新增 frontend/src/api/posts.js 封装社区接口。
3. Community.vue 包含：
   - 搜索框
   - 分类筛选
   - 最新/最热排序
   - 发布帖子按钮
   - 帖子卡片列表
   - 空状态提示
4. PostCard.vue 显示：
   - 标题
   - 正文摘要
   - 分类标签
   - 作者昵称
   - 发布时间
   - 点赞数
   - 评论数
   - 收藏数
5. PublishPost.vue 支持发布帖子，包含标题、分类、正文。
6. PostDetail.vue 支持查看详情、点赞、收藏、评论。
7. 页面风格温柔现代，卡片式布局，不要做成表格。
8. 社区模块不调用 AI，不生成 AI 评论。
```

验收标准：

- 用户能发布帖子。
- 社区能展示帖子列表。
- 用户能进入详情、评论、点赞、收藏。
- 空数据时有友好提示。

## 七、第 5 步：好友与私聊后端模块

目标：

- 完成普通用户好友和私聊功能。
- 第一版使用 HTTP 轮询，不做 WebSocket。
- 不调用 AI。

新增文件建议：

- `backend/app/models/friend.py`
- `backend/app/schemas/friend.py`
- `backend/app/routers/friends.py`
- `backend/app/routers/private_message.py`

新增表：

- `friend_requests`
- `friends`
- `private_messages`

提示词：

```text
请基于当前 FastAPI + SQLAlchemy + MySQL 项目开发好友与私聊后端模块。

要求：
1. 新增 backend/app/models/friend.py。
2. 新增 backend/app/schemas/friend.py。
3. 新增 backend/app/routers/friends.py。
4. 新增 backend/app/routers/private_message.py。
5. 数据库表包括：
   - friend_requests：好友申请表
   - friends：好友关系表
   - private_messages：私聊消息表
6. 所有接口需要 JWT 登录鉴权。
7. 实现接口：
   GET    /api/users/search
   POST   /api/friends/request
   GET    /api/friends/requests
   POST   /api/friends/accept
   POST   /api/friends/reject
   GET    /api/friends
   DELETE /api/friends/{friend_id}
   POST   /api/private-message/send
   GET    /api/private-message/history/{friend_id}
   GET    /api/private-message/unread
   POST   /api/private-message/read/{friend_id}
8. 私聊只保存真实用户之间的消息。
9. 私聊模块不调用 AI，不做情绪分析，不生成 AI 回复。
10. 需要判断双方是否为好友，非好友不能发送私聊消息。
```

验收标准：

- 用户能搜索其他用户。
- 用户能发送、同意、拒绝好友申请。
- 好友之间能发送私聊消息。
- 未读消息数量正确。

## 八、第 6 步：好友与私聊前端页面

目标：

- 完成好友列表、好友申请处理、用户私聊页面。

页面：

- `frontend/src/views/Friends.vue`
- `frontend/src/views/PrivateChat.vue`

建议组件：

- `frontend/src/components/ChatBubble.vue`
- `frontend/src/components/EmptyState.vue`

提示词：

```text
请开发好友与私聊前端页面。

要求：
1. 新增 frontend/src/api/friends.js。
2. 新增 frontend/src/api/privateMessage.js。
3. 完善 frontend/src/views/Friends.vue。
4. 完善 frontend/src/views/PrivateChat.vue。
5. Friends.vue 包含：
   - 搜索用户
   - 发送好友申请
   - 好友申请列表
   - 同意/拒绝好友申请
   - 好友列表
   - 未读消息提示
6. PrivateChat.vue 包含：
   - 左侧好友列表
   - 中间聊天窗口
   - 顶部显示好友昵称
   - 底部输入框和发送按钮
   - 聊天气泡左右区分
   - 使用 HTTP 轮询刷新消息
7. 私聊页面不要显示情绪分析面板。
8. 私聊页面不要显示 AI 回复按钮。
9. 私聊页面不要调用 AI 接口。
10. 页面风格与 Home.vue 保持一致。
```

验收标准：

- 用户能添加好友。
- 好友能进入私聊。
- 私聊消息能正常发送、刷新和显示。
- 页面明确区别于 AI 聊天。

## 九、第 7 步：后台管理模块

目标：

- 完成管理员基础管理能力。

后端文件：

- `backend/app/models/admin.py`
- `backend/app/schemas/admin.py`
- `backend/app/routers/admin.py`

前端页面：

- `frontend/src/views/Admin.vue`

提示词：

```text
请开发后台管理模块。

要求：
1. 后端新增 backend/app/routers/admin.py。
2. 管理员接口必须校验当前用户 role 是否为 admin 或 super_admin。
3. 实现接口：
   GET    /api/admin/users
   DELETE /api/admin/users/{user_id}
   PUT    /api/admin/users/{user_id}/disable
   GET    /api/admin/posts
   DELETE /api/admin/posts/{post_id}
   GET    /api/admin/comments
   DELETE /api/admin/comments/{comment_id}
   GET    /api/admin/statistics
4. 前端完善 frontend/src/views/Admin.vue。
5. Admin.vue 包含：
   - 统计卡片
   - 用户管理
   - 帖子管理
   - 评论管理
   - 搜索框
   - 禁用和删除按钮
6. 删除操作需要二次确认。
7. 普通用户不能访问后台。
8. 后台不开发 AI agent 管理功能。
```

验收标准：

- 管理员可以进入后台。
- 普通用户无法进入后台。
- 管理员可以管理用户、帖子和评论。

## 十、第 8 步：前后端联调与体验优化

目标：

- 让完整项目可演示、可运行、可验收。

需要完成：

- 统一页面视觉。
- 统一错误提示。
- 增加 loading 状态。
- 增加空状态组件。
- 删除操作增加确认。
- 修复接口边界错误。
- 补充测试账号。

提示词：

```text
请对当前项目进行整体联调和体验优化。

要求：
1. 检查所有前端页面是否能正常跳转。
2. 检查所有需要登录的页面是否会校验 token。
3. 检查 Axios 是否统一携带 Authorization Token。
4. 检查接口错误是否有 Element Plus 消息提示。
5. 给列表页面增加 loading 状态。
6. 给空数据页面增加 EmptyState 组件。
7. 删除帖子、评论、好友时增加二次确认。
8. 统一页面配色、卡片圆角、按钮风格。
9. 修复明显的接口异常和前端报错。
10. 不要开发 AI agent，不要接入阿里云百炼。
```

验收标准：

- 项目主要流程能完整演示。
- 页面视觉统一。
- 异常操作有提示。
- 空数据不尴尬。

## 十一、第 9 步：文档与交付整理

目标：

- 让项目能被老师或同学按文档跑起来。

需要补充：

- 后端运行说明。
- 前端运行说明。
- MySQL 建库语句。
- `.env.example`。
- 数据库初始化说明。
- 测试账号。
- 功能测试流程。
- 常见问题。
- 项目目录结构说明。

提示词：

```text
请根据当前项目代码补充完整 README 和交付说明。

要求：
1. 更新 backend/README.md。
2. 更新 frontend/README.md。
3. 在项目根目录新增 README.md。
4. 新增 backend/.env.example。
5. 文档包含：
   - 项目简介
   - 技术栈
   - 项目目录结构
   - MySQL 建库语句
   - 后端安装和启动步骤
   - 前端安装和启动步骤
   - 测试账号说明
   - 功能测试流程
   - 常见问题解决方法
6. 明确说明当前版本不包含 AI agent 开发，不接入阿里云百炼模型。
7. 文档语言清晰，适合课程设计提交。
```

验收标准：

- 新用户可以按 README 启动项目。
- 文档说明当前功能边界。
- 项目提交内容清晰。

## 十二、建议的最终交付范围

如果 AI agent 不在你的开发任务内，建议最终交付范围定义为：

- 用户注册登录与 JWT 鉴权。
- 温柔治愈风格首页。
- 个人中心。
- 社区广场、发帖、评论、点赞、收藏。
- 好友申请、好友列表、用户私聊。
- 后台用户、帖子、评论管理。
- 项目运行文档与测试流程。

不交付：

- 阿里云百炼模型接入。
- AI 情绪分析。
- AI 智能回复。
- AI 聊天记录和情绪记录模块。
- 风险等级识别。
- AI agent Prompt 工程。

如果首页仍保留“AI 情绪陪伴”入口，建议处理方式：

- 跳转到占位页 `/ai-chat`。
- 页面提示“该功能为后续扩展模块，当前版本暂未开放”。
- 不写模型调用代码。
- 不写假 AI 回复。
- 不把 AI 功能和私聊、社区混在一起。

## 十三、推荐下一条提示词

如果你现在要继续开发，建议下一步先做“工程整理与路由补全”。可以直接使用下面这条：

```text
请基于当前项目继续开发第一步：工程整理与前端路由补全。

要求：
1. 修改 frontend/src/router/index.js，补全 /profile、/friends、/private-chat、/community、/community/:id、/publish-post、/admin、/ai-chat 路由。
2. 新增对应的 Vue 页面文件，先做美观占位页。
3. 页面风格与 Home.vue 保持一致，使用温柔、治愈、现代的浅色卡片布局。
4. 未登录用户访问主要页面时跳转 /login。
5. /ai-chat 只做“后续扩展，占位未开放”页面，不开发 AI agent，不接入阿里云百炼。
6. 新增 .gitignore，忽略 node_modules、dist、__pycache__、*.log、.env。
7. 保证 npm run build 可以通过。
```

